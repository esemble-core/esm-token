/*var EsmRelayRecipient = artifacts.require("./EsmRelayRecipient.sol");


contract("EsmRelayRecipient", async accounts => {
  const FROM_ACCT = accounts[0];
  const TO_ACCT = accounts[1];

  it("deploys", async () => {
   const token = await EsmRelayRecipient.deployed();
  /*  const totalSupply = await token.totalSupply();

    assert.equal(
      totalSupply.toNumber(),
      FIVE_BILLION,
      "sets total supply to 5B"
    );
    //console.log("account[0]", accounts[0]);
    const adminAccountBalance = await token.balanceOf(accounts[0]);
    assert.equal(
      adminAccountBalance,
      FIVE_BILLION,
      "initial supply is sent to admin account"
    );
  });
});*/